# Recordings

Raw Playwright codegen recordings shared by the QA lead. Reference material
for building the real specs - NOT runnable as-is (codegen output has no
retry/wait handling and hardcodes run-specific document numbers).

- production-flow.recorded.ts : full Production E2E flow, recorded 29-08-2026.
  Flow: Concept (create + Uploads + Approval) -> Job Work (from concept) ->
  Job Assignment (Design And CAD / CAD Modeling) -> Process Movement Accept ->
  Worker Issue + Receipt (Prabhat) -> Process Movement Transfer (to Casting
  Process / Casting Inspection) + Accept -> Worker Issue + Receipt
  (Sioniquser11; item Gold,Ring-Tendulkar / 91.6 / 5.000g / Move to Job
  Finalize) -> Job Finalize (recording ends while selecting rows; Generate
  Barcode not captured).
